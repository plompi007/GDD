# ChainWorks — כללי ברזל

לפני כל שורת קוד: קרא את `docs/GDD.md` במלואו. הוא המקור היחיד לאמת על מכניקה, פיזיקה, UX וזהות חזותית.

## סטאק (מעודכן — v1.2, Rust)
Rust + `nannou` 0.19 (חלון/רינדור, קבוע ל-0.19 — לא 0.20+ שנכתב מחדש על Bevy) + `rapier2d` (עם `enhanced-determinism`) + `serde`/`serde_json`. דסקטופ קודם (Windows/Mac/Linux), בהשראת הארכיטקטורה של OpenTIM (ראה docs/GDD.md §2.1/§5.5) — קוד מקורי לגמרי, לא תלות/fork בקוד ה-GPL-3.0 שלהם. ניוד למובייל/App Store הוא שלב עתידי נפרד (nannou לא תומך במובייל — ראה §7).

## Milestones
עובדים לפי Milestones (`docs/GDD.md` סעיף 5.8). ממש רק את ה-Milestone המבוקש ועצור. אל תיגע בשום דבר מעבר לו.

## כללי ברזל

1. **קומפילציה נקייה, אפס `unwrap()`/`expect()` מחוץ ל-`main`/בדיקות** אלא אם יש הצדקה מפורשת בהערה (שגיאה שבאמת לא-אמורה-לקרות, כמו בניית קולידר עם פרמטרים תקינים בקומפייל-טיים).
2. **אפס `rand::random()`/`Math`-רנדומליות ואפס `Instant::now()`/`SystemTime::now()` בתוך `core::sim` ו-`core::graph`.** רנדומליות מותרת רק בשכבת הרינדור, מחוץ ל-tick הפיזיקלי.
3. **כל רכיב מוגדר ב-data JSON** (`data/parts/*.json`), לעולם לא ב-hard-code בתוך הקוד (למעט M0/M1 שהם smoke test מפורש לפני שקיים parts registry).
4. **`EditorState` הוא read-only במצב `RUNNING`.** המקור היחיד לאמת על מה שהשחקן הציב.
5. **אפס חפיפה משפטית, מקסימום נאמנות מכנית.** מכניקה, יחסי פיזיקה ועומק תוכן — קרוב ככל האפשר ל-The Incredible Machine (מותר, לא מוגן בזכויות יוצרים). שם, דמות, גרפיקה, פריסת שלב, טקסט שיווקי — מקורי ב-100%, בלי "בהשראת" מזוהה. ראה `docs/GDD.md` סעיף 4 לטבלת שמות אסורים/מותרים.
6. **מקסימום נאמנות ארכיטקטונית ל-OpenTIM, אפס תלות בקוד שלהם.** מבנה המודולים (`part.rs`, `level_file_format.rs`, `atmosphere.rs` וכו') נכתב מחדש כאן בהשראת הצורה שלהם, לא מועתק/מיובא מה-crate שלהם (שהוא GPL-3.0). ראה docs/GDD.md §2.1/§5.5 להסבר המלא.
7. אחרי כל Milestone: הרץ `cargo build && cargo test` והראה שהכול ירוק, ואז בקש commit נפרד עם תיאור ברור.
8. כשמשהו נשבר בפיזיקה: קודם טסט שמשחזר את הבאג, ורק אחר כך תיקון.

## דטרמיניזם — בדיקה תמידית
אחרי כל שינוי במנוע הפיזיקה או בגרף האנרגיה: הרצה של אותו שלב 600 ticks חייבת לתת מצב עולם זהה (השוואת translation/rotation של כל גוף, או hash של snapshot) ב-3 ריצות. זה טסט קיים מ-M1 ואילך (`tests/determinism.rs`) — אל תעקוף אותו.
