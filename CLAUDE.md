# ChainWorks — כללי ברזל

לפני כל שורת קוד: קרא את `docs/GDD.md` במלואו. הוא המקור היחיד לאמת על מכניקה, פיזיקה, UX וזהות חזותית.

## Milestones
עובדים לפי Milestones (`docs/GDD.md` סעיף 5.8). ממש רק את ה-Milestone המבוקש ועצור. אל תיגע בשום דבר מעבר לו.

## כללי ברזל

1. **TypeScript strict. אפס `any`.**
2. **אפס `Math.random()` ואפס `Date.now()` בתוך `core/sim` ו-`core/graph`.** רנדומליות מותרת רק בשכבת הרנדר, מחוץ ל-`tick()`.
3. **כל רכיב מוגדר ב-data JSON** (`data/parts/*.json`), לעולם לא ב-hard-code בתוך הקוד.
4. **`editorState` הוא read-only במצב `RUNNING`.** המקור היחיד לאמת על מה שהשחקן הציב.
5. **כל ערך אנימציה/צל/רדיוס/ריווח דרך `ui/tokens.ts` בלבד** (`MOTION`, `ELEVATION`, `RADIUS`, `SPACE`). אפס ערכים מומצאים inline בקומפוננטה.
6. **אפס חפיפה משפטית, מקסימום נאמנות מכנית.** מכניקה, יחסי פיזיקה ועומק תוכן — קרוב ככל האפשר ל-The Incredible Machine (מותר, לא מוגן בזכויות יוצרים). שם, דמות, גרפיקה, פריסת שלב, טקסט שיווקי — מקורי ב-100%, בלי "בהשראת" מזוהה. ראה `docs/GDD.md` סעיף 4 לטבלת שמות אסורים/מותרים.
7. אחרי כל Milestone: הרץ `pnpm test` והראה שהכול ירוק, ואז בקש commit נפרד עם תיאור ברור.
8. כשמשהו נשבר בפיזיקה: קודם טסט שמשחזר את הבאג, ורק אחר כך תיקון.

## דטרמיניזם — בדיקה תמידית
אחרי כל שינוי במנוע הפיזיקה או בגרף האנרגיה: `md5(world.createSnapshot())` חייב להישאר זהה אחרי 600 ticks, ב-3 ריצות. זה כבר טסט קיים (`tests/determinism.test.ts`) — אל תעקוף אותו.
