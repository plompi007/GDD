# ChainWorks — כללי ברזל

לפני כל שורת קוד: קרא את `docs/GDD.md` במלואו. הוא המקור היחיד לאמת על מכניקה, פיזיקה, UX וזהות חזותית.

## סטאק (מעודכן — v1.3, Bevy)

Rust + `bevy = "0.16"` (ECS/חלון/רינדור) + `bevy_rapier2d = "0.30"` (עם `enhanced-determinism`) + `serde`/`serde_json`. דסקטופ **ואנדרואיד** יחד מהתחלה — Bevy תומך בזה natively, בניגוד ל-`nannou` שנוסה קודם (v1.2) ונדחה כי הוא דסקטופ-בלבד. בהשראת הארכיטקטורה של OpenTIM (ראה docs/GDD.md §2.1/§2.1a/§5.5) — קוד מקורי לגמרי, לא תלות/fork בקוד ה-GPL-3.0 שלהם.

**⚠️ אל תשדרג `bevy`/`bevy_rapier2d` בלי לבדוק קודם.** גרסאות 0.19.x של `bevy_reflect` שבורות מול `glam 0.32.1`/`serde` (E0277 על `BVec3A`/`BVec4A`, regression ב-ecosystem, לא באג שלנו). 0.16/0.30 הוא הזוג היציב האחרון שאומת שמתקמפל ורץ. פרטים מלאים ותהליך האבחון ב-docs/GDD.md §2.1a — קרא אותו לפני כל ניסיון שדרוג.

## Milestones

עובדים לפי Milestones (`docs/GDD.md` סעיף 5.8). ממש רק את ה-Milestone המבוקש ועצור. אל תיגע בשום דבר מעבר לו. **M3.5 (בדיקת APK אנדרואיד אמיתית על המכשיר של המפתח) הוא לא אופציונלי** — זו נקודת הבדיקה הכי חשובה בתוכנית כולה.

## כללי ברזל

1. **קומפילציה נקייה, אפס `unwrap()`/`expect()` מחוץ ל-`main`/בדיקות** אלא אם יש הצדקה מפורשת בהערה (שגיאה שבאמת לא-אמורה-לקרות, כמו בניית קולידר עם פרמטרים תקינים בקומפייל-טיים).
2. **אפס `rand::random()`/`Math`-רנדומליות ואפס `Instant::now()`/`SystemTime::now()` בתוך `core::sim` ו-`core::graph`.** רנדומליות מותרת רק בשכבת הרינדור, מחוץ ל-tick הפיזיקלי.
3. **כל רכיב מוגדר ב-data JSON** (`data/parts/*.json`), לעולם לא ב-hard-code בתוך הקוד (למעט M0/M1 שהם smoke test מפורש לפני שקיים parts registry).
4. **`EditorState` (כ-Bevy Resource) הוא read-only במצב `RUNNING`.** המקור היחיד לאמת על מה שהשחקן הציב.
5. **אפס חפיפה משפטית, מקסימום נאמנות מכנית.** מכניקה, יחסי פיזיקה ועומק תוכן — קרוב ככל האפשר ל-The Incredible Machine (מותר, לא מוגן בזכויות יוצרים). שם, דמות, גרפיקה, פריסת שלב, טקסט שיווקי — מקורי ב-100%, בלי "בהשראת" מזוהה. ראה `docs/GDD.md` סעיף 4 לטבלת שמות אסורים/מותרים.
6. **מקסימום נאמנות ארכיטקטונית ל-OpenTIM, אפס תלות בקוד שלהם.** מבנה המודולים (`part.rs`, `level_file_format.rs`, `atmosphere.rs` וכו') נכתב מחדש כאן בהשראת הצורה שלהם, לא מועתק/מיובא מה-crate שלהם (שהוא GPL-3.0). ראה docs/GDD.md §2.1/§5.5 להסבר המלא.
7. **רכיב = ECS, לא trait object.** התנהגות רכיב (M2 ואילך) = marker component + Systems שנרשמים ל-`SimPlugin`, לא registry גנרי של trait objects. ראה docs/GDD.md §5.6.
8. אחרי כל Milestone: הרץ `cargo build && cargo test` והראה שהכול ירוק, ואז בקש commit נפרד עם תיאור ברור.
9. כשמשהו נשבר בפיזיקה: קודם טסט שמשחזר את הבאג, ורק אחר כך תיקון.

## דטרמיניזם — בדיקה תמידית

אחרי כל שינוי במנוע הפיזיקה או בגרף האנרגיה: הרצה של אותו שלב 600 ticks חייבת לתת מצב עולם זהה (השוואת translation/rotation של כל גוף) ב-3 ריצות. זה טסט קיים מ-M1 ואילך (`tests/determinism.rs`) — אל תעקוף אותו.
