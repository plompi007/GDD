{
  "name": "chainworks",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test:unit": "vitest run",
    "test:browser": "node scripts/browser-test-runner.mjs",
    "test": "npm run test:unit && npm run test:browser"
  },
  "dependencies": {
    "@dimforge/rapier2d-deterministic": "0.20.0",
    "pixi.js": "8.20.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "playwright": "1.63.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "3.6.0",
    "vitest": "^2.1.0"
  }
}
